# Mini‑proyecto POO — C++ (CMake)

## Requisitos
- CMake 3.20+
- Compilador C++17

## Comandos
```bash
# Configurar y compilar
cmake -S . -B build
cmake --build build -j

# Ejecutar binarios
./build/app
./build/tests
```
